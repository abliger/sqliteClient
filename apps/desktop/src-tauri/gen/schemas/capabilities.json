{
    "default": {
        "identifier": "default",
        "description": "Default capabilities for the SQLite Client",
        "local": true,
        "windows": ["main"],
        "permissions": ["core:default", "dialog:default"]
    }
}
